# social-face-receiver

To install dependencies:

```bash
bun install
```

To run:

```bash
bun dev
```

## How to deploy new version:
commit tag as `vX.Y.Z` and push it to the repository.

```bash
git tag -a v1.0 -m "v1.0"
```

```bash
git push origin v1.0
```


